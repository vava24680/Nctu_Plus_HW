<%= bootstrap_form_for @announcement, :url=> user_announcement_path(@announcement),:method=>:patch do |user_data| %>
    <%= render :partial=>"form" ,:locals=>{:data=>user_data} %>
    <br/>
    <%= user_data.submit "Update", class:'btn btn-success' %>
<% end %>